<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication_2" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyApplication_2" parent="Base.Theme.MyApplication_2" />
    
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/baseColor</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/applogo</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.MyApplication_2</item>
    </style>
</resources>